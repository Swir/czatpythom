# CzatPythom Status

<img width="100%" src="assets/readme/progress-mini.svg" alt="CzatPythom product progress — N/A because the repository has no canonical measurable product roadmap" />

| Item | Verified state |
|---|---|
| Product completion | **N/A** — no canonical measurable product roadmap or denominator exists |
| Latest public release | **v2.1.0** |
| Runtime | Python 3.10+ |
| UI | Rich terminal |
| Storage | Local JSON by default; optional GitHub repository backend |
| Progress source | This status file records the absence of a product denominator; it is not a second roadmap |

The release version, commit count, test count, elapsed time and documentation completeness are **not** used as product-completion percentages. If a canonical measurable roadmap is added later, the progress generator must be reconfigured to derive the SVG from that source.

Legacy ASCII/Unicode progress meters are retired. The SVG above is the progress visualization; ordinary numeric/status text is the fallback.
